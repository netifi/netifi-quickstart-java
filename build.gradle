subprojects {
    apply plugin: 'idea'

    group 'io.netifi.proteus.quickstart'
    version '1.0.0'

    // Proteus Versions
    ext {
        proteusVersion = '0.8.9'
    }

    repositories {
        jcenter()
    }
}