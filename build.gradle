subprojects {
    apply plugin: 'idea'

    group 'io.netifi.proteus.quickstart'
    version '1.0.0'

    // Proteus Versions
    ext {
        proteusVersion = '0.7.15'
    }

    repositories {
        jcenter()
    }
}